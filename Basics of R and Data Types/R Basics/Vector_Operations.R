#Arithmatic with vectors
v1 <- c(1,2,3)
v2 <- c(4,5,6)
print(v1+v2)
print(v1-v2)
print(v1/v2)
print(v1*v2)
print(v1^v2)

#Add everything in vector
sum.of.vector <- sum(v1)
print(sum.of.vector)

#Check mean of a vector
mean(v1)

#Check the std deviation
sd(v2)

#Check the Variance
var(v)

#Get maximum value
max(v2)

#Get minimum value
min(v1)

#Get product of the elements
prod(v1)